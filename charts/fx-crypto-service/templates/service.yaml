apiVersion: v1
kind: Service
metadata: { name: fx-crypto-service }
spec:
  selector: { app: fx-crypto-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
