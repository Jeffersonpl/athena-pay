apiVersion: v1
kind: Service
metadata: { name: accounts-service }
spec:
  selector: { app: accounts-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
