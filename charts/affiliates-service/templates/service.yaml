apiVersion: v1
kind: Service
metadata: { name: affiliates-service }
spec:
  selector: { app: affiliates-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
