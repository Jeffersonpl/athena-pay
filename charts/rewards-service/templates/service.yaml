apiVersion: v1
kind: Service
metadata: { name: rewards-service }
spec:
  selector: { app: rewards-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
