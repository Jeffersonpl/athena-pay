apiVersion: v1
kind: Service
metadata: { name: wire-service }
spec:
  selector: { app: wire-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
