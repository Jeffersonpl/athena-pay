apiVersion: v1
kind: Service
metadata: { name: boleto-service }
spec:
  selector: { app: boleto-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
