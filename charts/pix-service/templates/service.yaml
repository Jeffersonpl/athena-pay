apiVersion: v1
kind: Service
metadata: { name: pix-service }
spec:
  selector: { app: pix-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
