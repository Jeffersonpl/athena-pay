apiVersion: v1
kind: Service
metadata: { name: kyc-service }
spec:
  selector: { app: kyc-service }
  ports: [ { port: { { .Values.service.port } }, targetPort: { { .Values.service.targetPort } } } ]
